////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
Bonjour !!
Pour l'exercice j'ai fait un champ texte qui récupere la valeur de ce champ, pour l'inserer dans l'url .
Au submit on execute la fonction asynchrone getWeather qui fera la requête à l'Api.
Les datas sont ensuite traiter pour être afficher, la température est arrondie via la méthode Math.round.
La température récuperer est utiliser afin de définir si on applique un fond chaud > 15C ou froid < 15 C grâce à une condition.

Merci

La branche à utiliser est la final .

Pour commencer, git clone url puis git fetch --all, git checkout final, puis lancer yarn dans le terminal après l'installation, yarn start dans le terminal.


